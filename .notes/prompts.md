
On game-canvas for example we repeat a lot of code to create the event, we can create a function that do this and populate with the default fields.

For now we dont have a way to change the step of the hero journey. We need a way to do this on narrative events.